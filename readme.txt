   
Inputs :
Images.csv 		 : Contains the images which are represented by an image ID and the corresponding class.
EdgeHistogram.csv  : Contains the feature data for the Edge Histogram feature for the images.

pip install notebook
In order to manage multiple Python versions can use conda virtual environment	
	conda create --name <env> --file requirements.txt 
	conda activate <env>
Need to install the necessary libraries .
	pip install pandas
	pip install numpy
		
*Initially few libraries are evoked :
*pandas is used to analyse data
*numpy is used to do arithmetic operations for the arrays
*matplotlib.pyplot is used for graphical plotting
